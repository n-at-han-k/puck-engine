# frozen_string_literal: true

PuckEngine.configure do |config|
  # Configure default components available in the editor
  # config.default_components = [:heading, :paragraph, :button, :card]
  
  # Configure additional custom components
  # config.custom_components = []
  
  # Configure editor permissions
  # config.authorize_editor = ->(user) { user.admin? }
  
  # Configure page rendering options
  # config.layout = "application"
end
